# TechPark-Visitor-Entry-Management
“This project is built using Spring Framework and Spring Boot to create CRUD REST APIs that handle data in JSON format. MySQL is used as the database for storage, and Postman is used for testing the API endpoints. The application ensures efficient data management and provides a clean, scalable backend structure.”
